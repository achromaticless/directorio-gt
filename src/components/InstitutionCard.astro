---
interface Props {
    name: string;
    email: string;
}

const { name, email } = Astro.props;
---

<div class="">
    <div class="text-base">{name}</div>
    <div class="text-sm text-black-50">{email}</div>
</div>